1. **Ajax:** 全称Asynchronous JavaScript And XML，异步的JavaScript和XML。其作用有如下2点：
	- 与服务器进行数据交换：通过Ajax可以给服务器发送请求，并获取服务器响应的数据。
	- 异步交互：可以在**不重新加载整个页面**的情况下，与服务器交换数据并**更新部分网页**的技术，如：搜索联想、用户名是否可用的校验等等。
2. 同步请求与异步请求的区别：
	针对于上述Ajax的局部刷新功能是因为Ajax请求是异步的，与之对应的有同步请求。接下来我们介绍一下异步请求和同步请求的区别。
	- **同步请求**发送过程如下图所示：
	![](https://heuqqdmbyk.feishu.cn/space/api/box/stream/download/asynccode/?code=MzE0NGI4NDZlNWI3MjM2Y2YyMjdhN2E5YmRjMjc3NGZfeWhQV1MzRmRWVllaT1ZCQWVyUkRoZ004cFhIU3BHb3NfVG9rZW46UjRPZmJKdWh6b2JPeDd4MVBTZGNLZWlOblJiXzE3NjQzMTk2MTY6MTc2NDMyMzIxNl9WNA)
	浏览器页面在发送请求给服务器，在服务器处理请求的过程中，浏览器页面不能做其他的操作。只能等到服务器响应结束后才能，浏览器页面才能继续做其他的操 作。
	- **异步请求**发送过程如下图所示：
	![](https://heuqqdmbyk.feishu.cn/space/api/box/stream/download/asynccode/?code=NTAwNjBjMWEyM2QxOWQyMjAyYzgzMWNlOWJmZjIyZGJfRnpYQXRvYmJVNjR3ZTlQeEhSR1J3M3ljTFhaallnTEJfVG9rZW46VzZWN2J4dTNkb0NNM3Z4VFJTa2NUN2lMbmtnXzE3NjQzMTk2MTY6MTc2NDMyMzIxNl9WNA)
	浏览器页面发送请求给服务器，在服务器处理请求的过程中，浏览器页面还可以做其他的操作。
3. Axios是一门更加简单的发送Ajax请求的技术 。Axios是对原生的AJAX进行封装，简化书写。Axios官网是：`https://www.axios-http.cn`
4. Axios入门程序：
	1) 引入Axios文件（如果网络不通畅，可以使用离线的已经下载好的js文件，资料中已经提供）
	```HTML
	<script src="https://unpkg.com/axios/dist/axios.min.js"></script>
	```
	2) 点击按钮时，使用Axios发送请求
	```HTML
	<!DOCTYPE html>
	<html lang="en">
	<head>
	  <meta charset="UTF-8">
	  <meta name="viewport" content="width=device-width, initial-scale=1.0">
	  <title>Axios入门程序</title>
	</head>
	<body>
	
	  <button id="getData">GET</button>
	  <button id="postData">POST</button>
	  
	  <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
	  <script>
	    //GET请求
	    document.querySelector('#getData').onclick = function() {
	      axios({
	        url:'https://mock.apifox.cn/m1/3083103-0-default/emps/list',
	        method:'get'
	      }).then(function(res) { // 请求成功后要调用的函数，res中存放请求中的数据
	        console.log(res.data); 
	      }).catch(function(err) { // 请求失败后调用的函数，err中存放失败原因
	        console.log(err);
	      })
	    }
	    
	    //POST请求
	    document.querySelector('#postData').onclick = function() {
	      axios({
	        url:'https://mock.apifox.cn/m1/3083103-0-default/emps/update',
	        method:'post'
	      }).then(function(res) {
	        console.log(res.data);
	      }).catch(function(err) {
	        console.log(err);
	      })
	    }
	  </script>
	</body>
	</html>
	```
5. 请求方法别名
	`axios.请求方式(url [, data [, config]])`
	具体如下：

| 方法                                 | 描述         |
| ---------------------------------- | ---------- |
| axios.get(url [, config])          | 发送get请求    |
| axios.delete(url [, config])       | 发送delete请求 |
| axios.post(url [, data[, config]]) | 发送post请求   |
| axios.put(url [, data[, config]])  | 发送put请求    |
6. 时刻注意，ajax（或者axios）发起的都是**异步请求**。发起请求后，程序的运行不会被阻塞，而是直接向下运行，负责处理该请求的子线程稍后返回结果并调用回调函数。
7. 如果使用axios中提供的.then(function(){....}).catch(function(){....})，这种回调函数的写法，会使得代码的可读性和维护性变差。 而为了解决这个问题，我们可以使用两个关键字，分别是：**async、await**。
	可以通过async、await可以让异步变为同步操作。async就是来声明一个异步方法，await是用来等待异步任务执行。**注意，await关键字仅在async关键字修饰的函数中才会起作用。**
	**代码修改前：**
	```JavaScript
	search() {
	    //基于axios发送异步请求，请求https://web-server.itheima.net/emps/list，根据条件查询员工列表
	    axios.get(`https://web-server.itheima.net/emps/list?name=${this.searchForm.name}&gender=${this.searchForm.gender}&job=${this.searchForm.job}`).then(res => {
	      this.empList = res.data.data
	    })
	  },
	```
	**代码修改后：**
	```JavaScript
	  async search() {
	    //基于axios发送异步请求，请求https://web-server.itheima.net/emps/list，根据条件查询员工列表
	    const result = await axios.get(`https://web-server.itheima.net/emps/list?name=${this.searchForm.name}&gender=${this.searchForm.gender}&job=${this.searchForm.job}`);
	    this.empList = result.data.data;
	  },
	```
	修改后，代码就变成同步操作了，一行一行的从前往后执行。 在前端项目开发中，经常使用这两个关键字配合，使得代码的可读性和可维护性变高。